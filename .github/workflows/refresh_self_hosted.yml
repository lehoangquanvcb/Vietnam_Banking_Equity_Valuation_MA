name: Refresh Banking Valuation Data
on:
  workflow_dispatch:
  schedule:
    - cron: '30 1 * * 1-5'
jobs:
  refresh:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Refresh Vnstock Bronze and build valuation
        shell: cmd
        run: RUN_UPDATE_AND_PUSH.bat
